[English](README.md) | [Русский](README.ru.md)

# Raidwise

Аддон для подготовки к рейду в **Wrath of the Lich King 3.3.5a** (`Interface: 30300`): составы группы и рейда, история встреч, КД на аккаунте и экспорт персонажа в JSON.

![](https://img.shields.io/badge/current_version-1.5.0-purple)
![](https://img.shields.io/badge/last_updated-2026--08--18-blue)


## Установка

1. Скопируйте папку `Raidwise` в каталог аддонов клиента:

   ```text
   <WoW>/Interface/AddOns/Raidwise/
   ```

2. Перезапустите клиент (или `/reload`).
3. При необходимости включите **Raidwise** на экране выбора персонажа (AddOns).
4. Если раньше стоял **mrc-exporter**, удалите эту папку из AddOns, чтобы загружался только Raidwise.


## Использование

Команды в игре:

| Команда | Описание |
|---------|-------------|
| `/raidwise` или `/rw` (также `/rw help`) | Показать справку |
| `/raidwise version` | Показать версию аддона |
| `/raidwise status` | Показать статус загрузки |
| `/raidwise show` | Открыть главное окно |
| `/raidwise hide` | Закрыть главное окно |

Простые панели, **левое меню** и страница содержимого.
В строке состояния — имя аддона и версия.
Esc или **X** в заголовке закрывает окно.

Вкладка **КД персонажей**:

- Таблица рейдовых и подземельных КД всех персонажей, сохранённых на этом аккаунте
- Первая колонка — название инстанса и тип (10 / 10 Героический / 25 / 25 Героический, плюс старые 20 и 40)
- Остальные колонки — персонажи (имя цветом класса, иконка спека и время последней проверки); зайдите на каждого альта, чтобы записать данные
- В сохранённых ячейках показано время до сброса

Вкладка **Экспорт экипировки и КД**:

- Краткое описание, затем **Включать названия предметов**
- **Экспорт персонажа** заполняет JSON (name, class, spec, gearScore, gear, bags, lockouts)
- **Выделить всё** выделяет JSON для Ctrl+C
- `gearScore` берётся из аддона **GearScore**, если он установлен (необязательная зависимость)

Вкладка **Состав группы**:

- Таблица текущей группы из 5 игроков (только вы, если вы не в группе)
- Строка над таблицей: средний iLvl и средний GearScore
- Колонки: имя, иконка класса, иконка спека, иконки рейд-баффов, GearScore, средний iLvl, карма, теги, гильдия с рангом
- **Обновить** заново считывает GearScore и осматривает ближайших игроков для обновления спека

Вкладка **Состав рейда**:

- Два блока: рейдовые группы **1–5**, затем **6–8**
- Строка над карточками: общий средний GearScore; вторая строка — число и средний GS по ролям (`Танки: 2 (6200 gs)`)
- Каждая группа — колонка из пяти карточек: класс + имя, роль + спек + GS/iLvl, иконки рейд-баффов, карма и теги
- Иконка роли как в RaidBuffStatus (танк / лекарь / ближний бой / дальний бой)
- Иконки баффов зависят от спека и расы (наведите курсор, чтобы увидеть название)
- Клик по заполненной карточке открывает **Профиль персонажа** (`{имя} - Профиль персонажа`)
- **Обновить** заново считывает GearScore и осматривает ближайших игроков для иконок спека

Вкладка **История**:

- Таблица игроков, с которыми вы были в группе или рейде (хранится в `RaidwiseDB.history`, остаётся после выхода)
- Колонки: имя, иконка класса, иконка спека, GearScore, iLvl, где встретили, когда, гильдия
- Клик по строке открывает **Профиль персонажа** (GUID, зона встречи, время и реалм)
- **Обновить** снова записывает текущую группу и перерисовывает список

Вкладка **Настройки**:

- Язык интерфейса: **English** или **Русский**
- Выбор сохраняется на аккаунте (`RaidwiseDB.locale`); на русском клиенте по умолчанию русский

Вкладка **Справка**:

- Что делает аддон (составы, история, КД, экспорт) и какие есть слэш-команды
- URL репозитория в поле копирования с кнопкой **Выделить всё** (Ctrl+C)

Схемы окон: [`docs/UI-Views.md`](docs/UI-Views.md). Размеры в пикселях: [`docs/UI-Sizes.md`](docs/UI-Sizes.md).

Потребители экспорта могут типизировать JSON через `types/CharacterExport.ts` и `types/CooldownsExport.ts`.

## Скриншоты

Главное окно (`/raidwise show`):

![Главное окно](./screenshots/main-view.png)

Меню в чате (`/raidwise help`):

![Меню в чате](./screenshots/in-chat-menu.png)

# Разработка

## Структура

```text
Raidwise/
  Raidwise.toc        # метаданные аддона (Interface 30300)
  Raidwise.lua        # точка входа, события, слэш-команды
  Locale.lua          # строки интерфейса EN/RU и переключение языка
  CharacterExport.lua # JSON-экспорт персонажа (экипировка, сумки, КД)
  CharacterLockouts.lua # снимки КД аккаунта для таблицы кулдаунов
  PartyRoster.lua     # статы участников группы / рейда
  RaidRoles.lua       # роли и рейд-баффы по спеку / расе
  PlayerHistory.lua   # сохранённый список встреч (вкладка История)
  ExporterWindow.lua  # главное окно в игре
docs/
  UI-Views.md         # ASCII-схемы каждого вида
  UI-Sizes.md         # размеры окна и элементов
types/
  CharacterExport.ts  # TypeScript-типы JSON экспорта персонажа
  CooldownsExport.ts  # TypeScript-типы JSON экспорта КД аккаунта
```

## Заметки

- Целевая сборка: **3.3.5a** (клиенты приватных серверов используют `## Interface: 30300`).
- Сохранённые переменные лежат в `RaidwiseDB` (`WTF/Account/.../SavedVariables/`). Настройки старого `MrcExporterDB` переносятся при первой загрузке. КД персонажей для таблицы кулдаунов — в `RaidwiseDB.characters`. Встречи в группе и рейде — в `RaidwiseDB.history` (ключ — GUID). Язык интерфейса — `RaidwiseDB.locale` (`enUS` или `ruRU`).
- `## X-LastUpdated` в `.toc` задаётся вручную; бейдж в README нужно держать в синхроне.
- Необязательная зависимость: **GearScore** (`## OptionalDeps`) для поля `gearScore` в экспорте.
