[English](README.md) | [Русский](README.ru.md)

# Raidwise

Аддон для подготовки к рейду в **Wrath of the Lich King 3.3.5a** (`Interface: 30300`): составы группы и рейда, анализ состава, рейтинг игроков, история встреч, КД на аккаунте и экспорт персонажа в JSON.

![](https://img.shields.io/badge/current_version-1.26.0-purple)
![](https://img.shields.io/badge/last_updated-2026--09--22-blue)


## Установка

1. Скопируйте папку `Raidwise` в каталог аддонов клиента:

   ```text
   <WoW>/Interface/AddOns/Raidwise/
   ```

2. После установки новых файлов или обновления TOC полностью перезапустите клиент. Для изменений уже загруженных файлов достаточно `/reload`.
3. При необходимости включите **Raidwise** на экране выбора персонажа (AddOns).
4. Если раньше стоял **mrc-exporter**, удалите эту папку из AddOns, чтобы загружался только Raidwise.


## Использование

Команды в игре:

| Команда | Описание |
|---------|-------------|
| `/raidwise` или `/rw` | Открыть главное окно |
| `/raidwise close` или `/rw close` | Закрыть главное окно |
| `/raidwise gearcheck` или `/rw gearcheck` | Открыть проверку экипа (цель) и сканировать |
| `/rw gearcheck summary` (также `items`, `enchants`, `gems`, `ok`) | Печать отчёта в канал отчётов (сначала сканирует при необходимости) |
| `/rw gearcheck raid dump` | Открыть состав рейда и показать последний dump проверки экипа рейда для копирования |
| `/rw gearcheck test` | Офлайн self-test правил |
| `/rw diagnose`, `/raidwise diagnose` или `/raidwisediag` | Проверка открытия окна и правил экипировки; отчёт для копирования |

Запускайте диагностику сразу после `/reload`, вне боя, чтобы проверить создание окна. Ctrl+A / Ctrl+C копирует отчёт. После установки новых Lua-файлов перезапустите клиент, предварительно обновив всю папку `Raidwise`. Проверка из CLI: `npm run diagnose`, отчёт — `diagnostics/latest.txt`; подробнее в [tests/README.md](tests/README.md).

Простые панели, **левое меню** (группы Личное / Рейд / Прочее) и страница содержимого.
В заголовке меню — **Raidwise** и версия аддона.
Esc или **X** в заголовке закрывает окно.

Перетаскиваемая кнопка миникарты открывает **Состав рейда** левой кнопкой мыши и **КД персонажей** правой. Подсказка показывает готовность рейда, настои/еду и КД персонажей аккаунта.

Вкладка **КД персонажей**:

- Таблица рейдовых и подземельных КД всех персонажей, сохранённых на этом аккаунте
- Первая колонка — название инстанса и тип (`(Рейд)` / `(Подземелье)`); размер и режим — в ячейках персонажей
- Остальные колонки — персонажи (имя цветом класса, иконка спека и время последней проверки); зайдите на каждого альта, чтобы записать данные
- **Удалить** у колонок персонажей (кроме текущего) убирает данные из таблицы (повторный вход восстанавливает данные)
- В сохранённых ячейках — метки размера/режима (`10`, `10h`, `25`, `25h`; героик красным); наведение показывает время до сброса
- Строка **Валюта** внизу: в первой колонке подписи и сумма по аккаунту; в колонках персонажей — иконка и количество (золото, эмблемы, честь/арена и прочие жетоны)

Вкладка **Экспорт экипировки и КД**:

- Краткое описание, затем **Включать названия предметов**
- **Экспорт персонажа** заполняет JSON (name, class, spec, gearScore, gear, bags, lockouts)
- **Выделить всё** выделяет JSON для Ctrl+C
- `gearScore` берётся из аддона **GearScore**, если он установлен (необязательная зависимость)

Вкладка **Состав рейда**:

- Два блока: рейдовые группы **1–5**, затем **6–8**; вне рейда ваша группа занимает слот 1
- Над сеткой: компактная шапка (чипы S·A·B·C·D, GS и роли, настой/еда/камни/броня/оружие/чары с иконкой отчёта, иконки Сканировать/Экспорт/Обновить/К составу); затем статус скана и прогресс
- Карточка: класс + имя, иконки настоя/еды, роль + спек + GS/iLvl, компактные `P:`/`K:` рейтинги, отдельные оценки камней/брони/оружия/чар, **Экип** / **Перескан** и иконки меча/камня для отчётов
- Иконки меча/камня отправляют проблемы экипировки/оружия или камней/чар игрока в выбранный канал отчётов, всегда кратко (`CODE - slot,slot; CODE - slot`); наведение показывает текст сообщения
- После успешного полного скана рядом с сообщением о завершении показаны дата и время; перескан отдельного игрока не меняет эту отметку
- Наведение: мнение, теги, Karma, **гильдия (ранг)**, рейд-баффы, детали gear check
- **Сканировать** / **Экспорт всех** для проверки экипа рейда; **Экспорт всех** открывает текст; **К составу** закрывает его; клик по тексту + Ctrl+C копирует
- **Обновить** заново считывает GearScore и осматривает ближайших игроков
- Клик по карточке открывает **Профиль персонажа**

Вкладка **Анализ состава**:

- Чеклист текущей группы или рейда (вне группы — только вы)
- Под классами показаны иконки и количество обнаруженных специализаций; наведение перечисляет игроков
- Сверху: **Роли** и все 10 **Классов** со счётчиками; ниже разделы: Аггро, баффы, внешние баффы, снижение урона, дебаффы, восполнение маны, восполнение здоровья
- Золотые строки уже есть, серые — не хватает. В заголовке секции — сколько есть / всего (красный, если в секции ничего нет)
- Наведите на строку: кто даёт эффект и какой класс/спек какое заклинание; **Shift+клик** пишет эффект в канал отчётов (шапка окна)
- **Сообщить** — отсутствующие классы в канал отчётов (шапка окна); **Обновить** заново считывает группу (тот же осмотр, что у состава рейда)
- Подсказки показывают текст отчёта; отчёты эффектов содержат ссылки на заклинания и помещаются в одно сообщение до 255 байт.
- Полный список: [`docs/Raid-Composition.md`](docs/Raid-Composition.md)

Вкладка **Проверка экипа (цель)**:

- **Сканировать** оценивает цель или вас (итог S / A / B / C / D, иконки класса/спека, GS/iLvl, находки по фильтрам: Все / Предметы / Чары / Камни / B)
- Ранги спека: **preferred** / **acceptable** / **unwanted** / **forbidden** (соответствуют A / B / C / D). **S** = ID предмета есть в опубликованных BiS-списках спека (не уникальный выбор)
- Кнопки **Отчёт …** и `/rw gearcheck summary|items|enchants|gems|ok` печатают в **канал отчётов** (шапка окна; по умолчанию Авто = рейд/группа)
- Кнопки отчётов имеют иконки и превью; сводка показывает количество всех оценок, а проблемы камней сгруппированы по теме без ID.
- **Показать текстом** — сырой dump; **Сохранить отчёт** — снимок (~14 дней)
- Поверхностная оговорка; правила и известные ложные срабатывания: [`docs/Gear-Check-Progress.md`](docs/Gear-Check-Progress.md)
- `/rw gearcheck` открывает вкладку и сканирует; `/rw gearcheck test` — офлайн self-test

Вкладка **История**:

Сканы цели также записывают игрока в Историю. Во вкладке проверки экипировки кнопка **Профиль персонажа** открывает профиль проверенного игрока.

- Недавние встречи в группе, рейде и при проверке цели; фильтры по имени, классу и гильдии. Неиспользуемые записи без сохранённой карточки удаляются через 14 дней.
- Место встречи показано в подсказке строки.
- Клик по строке открывает **Профиль персонажа** (GUID, зона встречи, время и реалм)
- **Обновить** снова записывает текущую группу и перерисовывает список

**База персонажей** хранит изменённые вручную и импортированные карточки, показывает источник и фильтры по имени, классу, гильдии, личному мнению и источнику. Персонажа можно добавить по имени без сканирования. Отдельную строку можно удалить, а очистка всей базы требует подтверждения. Сохранённые карточки не удаляются при очистке встреч.

**Синхронизация** поддерживает явный обмен Игрок—Игрок и Игрок—Внешний источник:

- Вкладка **Синхронизация** ищет персонажа по имени и позволяет отправить одного персонажа или всю сохранённую базу цели, в гильдию или рейд.
- Кнопка **Поделиться** в открытом профиле и кнопка общего доступа в базе открывают одинаковый список получателей.
- Выбранные данные и базу можно экспортировать в копируемый JSON. Вставьте JSON от игрока или веб-приложения, просмотрите список изменений, затем выберите **Применить** или **Отмена**.
- Входящие запросы требуют согласия; их можно отклонить, игнорировать по имени персонажа независимо от реалма или полностью отключить. Список игнорируемых персонажей поддерживает поиск, страницы и **Снять игнор**.
- JSON синхронизации содержит `reportVersion: 1`; личные мемо остаются локальными и не передаются.

В стандартных списках друзей, игнорирования и писем видны отметки мнения. В обоих режимах списка гильдии, включая ElvUI, строки имеют светло-зелёный, светло-серый или светло-красный фон. Метки чата — зелёные, белые или красные; сообщения отрицательно оценённых игроков остаются красными. `/rw diagnose` проверяет загрузку модуля отметок и поиск персонажей гильдии.

После установки новых файлов или обновления TOC полностью перезапустите клиент; `/reload` подходит для изменений уже загруженных файлов.

**Рейтинг игрока** в профиле персонажа:

- Вкладки: **История**, **Изменить заметку**, **Факты**, **События**, **Мемо**, **Персонажи** — по умолчанию открывается **История**
- **Персонажи** связывает персонажей одного игрока: один локальный мейн и несколько альтов. Мнение общее, теги, факты, события и заметки остаются раздельными. История записывает связи, отвязки и смену мейна; подсказки перечисляют связанных персонажей. Мейн — локальный выбор, исключённый из данных для обмена. Сам обмен между игроками пока не реализован. Подробнее: [Reputation.md](docs/Reputation.md).
- Во вкладке **Изменить заметку** выберите **Положительное** / **Нейтральное** / **Отрицательное** и личные теги (до 3 в категории); **Сохранить и обновить** записывает мнение, теги, факты и события
- Во вкладке **Факты** — роли / идентичность (до 4)
- Во вкладке **История** — **Встретили**, **Был в одной группе** и журнал изменений с иконками
- Во вкладке **События** выберите тип по категории (**Посещаемость**, **Лут**, **Помощь**, **Поведение** — у каждой своя иконка) и **Добавить событие** / **Удалить** (черновик до **Сохранить и обновить**; контекст зоны при добавлении). Вступление в группу или рейд также записывает **В одной группе**, когда счётчик встреч увеличивается (первая встреча или ≥30 минут с прошлого раза)
- Во вкладке **Мемо** — личный свободный текст с **Сохранить** / **Сброс** (не передаётся и не пишется в Историю)
- В составе рейда и Истории показываются сохранённые мнение и сводка по тегам; клик по строке или карточке открывает профиль
- **Karma** пока показана как мок для будущего обмена через аддон или веб-приложение

Управление отчётами в шапке окна:

- **Канал отчётов**: куда пишутся отчёты состава рейда, анализа состава и проверки экипа (`RaidwiseDB.reportChannel`; по умолчанию Авто = рейд в рейде, группа в группе)
- **Форма отчёта проверки экипа**: Короткая (по умолчанию, меньше строк в чат) или Полная (`RaidwiseDB.reportForm`)
- Выбор канала виден только в составе рейда, анализе состава и проверке экипа; Short / Full — только в проверке экипа. Подписи каналов окрашены цветами чата.

Вкладка **Настройки**:

- Ссылка на changelog в репозитории с кнопкой выделения для копирования

- Категория **Тема**: светлая или тёмная, сохраняется на аккаунте в `RaidwiseDB.theme`

- Язык интерфейса: **English** или **Русский**
- Выбор сохраняется на аккаунте (`RaidwiseDB.locale`); на русском клиенте по умолчанию русский
- **Стартовая страница**: какая вкладка левого меню открывается по `/raidwise` (`RaidwiseDB.startupTab`; по умолчанию КД персонажей; **Справку** выбрать нельзя)
- Переключатели строк в подсказках игроков: скрыть личное мнение / личные теги / Karma / теги Karma (`RaidwiseDB.tooltip`)
- Превью компактного (живой) и столбикового варианта подсказки

Позиция кнопки миникарты сохраняется в `RaidwiseDB.minimapAngle`. При отрицательном личном мнении текст сообщения и маркер `<Rw>` красные; ссылки на предметы и достижения сохраняют исходные цвета. При положительном/нейтральном мнении маркер остаётся мятным/лавандовым с белыми скобками, текст — цвета канала.

Вкладка **Справка**:

- Краткий обзор (одно предложение на строку, со списками) и блоки по пунктам меню (те же иконки, у каждого свой layout `vN`)
- URL репозитория в поле копирования с кнопкой **Выделить всё** (Ctrl+C)

Схемы окон: [`docs/UI-Views.md`](docs/UI-Views.md). Размеры в пикселях: [`docs/UI-Sizes.md`](docs/UI-Sizes.md). Модель репутации: [`docs/Reputation.md`](docs/Reputation.md). Что проверяет анализ состава: [`docs/Raid-Composition.md`](docs/Raid-Composition.md).

Потребители могут типизировать JSON вкладки Export через `types/CharacterExport.ts`. `types/CooldownsExport.ts` описывает форму JSON КД аккаунта (`FormatCooldownsExport`; пока без кнопки в UI). `types/GearCheck.ts` — нормализованный отчёт Gear Check (`schemaVersion` 3).

## Скриншоты

Главное окно (`/raidwise`):

![Главное окно](./screenshots/main-view.png)

# Разработка

## Структура

```text
Raidwise/
  Raidwise.toc        # метаданные аддона (Interface 30300)
  Raidwise.lua        # точка входа, события, слэш-команды
  Locale.lua          # строки интерфейса EN/RU и переключение языка
  ChatReports.lua     # shared final chat message preparation
  CharacterExport.lua # JSON-экспорт персонажа (экипировка, сумки, КД)
  CharacterLockouts.lua # снимки КД аккаунта для таблицы кулдаунов
  InspectCoordinator.lua # shared inspect ownership, identity, and deadlines
  PartyRoster.lua     # статы участников группы / рейда
  RosterRefresh.lua   # coalesced refreshes and shared per-pass roster snapshots
  RaidRoles.lua       # роли и рейд-баффы по спеку / расе
  RaidComposition.lua # покрытие баффов, дебаффов и утилиты в группе / рейде
  PlayerHistory.lua   # каталоги рейтингов, нормализация и чтение
  PlayerHistoryStore.lua # history persistence and legacy migration
  RatingPresentation.lua # rating labels, tooltips, and chat marks
  ProfileDraft.lua    # plain profile draft editing model
  ProfilePanels.lua  # построение вкладок профиля и история
  CharacterLinks.lua # связи персонажей и локальный выбор мейна
  ProfileCharacters.lua # вкладка персонажей в профиле
  Diagnostics.lua    # проверки и независимое окно отчёта
  Minimap.lua         # кнопка миникарты и подсказка с готовностью рейда и КД
  UnitTooltips.lua    # строки личного/общественного рейтинга в подсказках
  ClassicOpinionMarkers.lua # отметки мнения в друзьях, игноре, почте и гильдии
  UITheme.lua         # theme palettes and bound colors/text
  UIWidgets.lua       # общие панели, кнопки, иконки, бейджи версии вёрстки
  RosterWidgets.lua   # roster, rating, and gear presentation helpers
  CharacterProfile.lua # профиль персонажа (мнение, теги, заметки, история)
  SyncData.lua         # версия JSON профиля, импорт и предварительный просмотр
  SyncTransport.lua    # предложения обмена, согласие, игнор и маршрутизация
  GearCheckCatalog.lua # каталоги чар / камней
  GearCheckSets.lua   # id кусков T9/T10 (информационно)
  GearCheckTrinkets.lua # пулы тринкетов по роли
  GearCheckProfiles.lua # профили классов + 30 спеков
  GearCheckBis.lua    # сгенерированные BiS item ID по спеку (оценка S)
  GearCheckRules.lua  # находки, активация мета-камней и ревизия правил
  GearCheckReport.lua # совместимость отчётов и полнота сканирования
  GearCheckGrades.lua # slot, category, and overall grades
  GearCheckExplanations.lua # category tooltips and grade explanations
  GearCheckSelfTest.lua # offline and in-game rule fixtures
  GearCheckCollector.lua # item/gem reads and normalized snapshots
  GearCheckSavedReports.lua # ручное сохранение / загрузка / очистка (~14 дней)
  GearCheck.lua       # управление сканированием и последним отчётом
  GearCheckReports.lua # gear report formatting and final messages
  GearCheckDump.lua    # text dumps and asynchronous raid export jobs
  PageCooldowns.lua   # вкладка Character cooldowns
  PageExport.lua      # вкладка Export gear and CDs
  PageRaid.lua        # вкладка Raid roster
  PageComposition.lua # вкладка Raid composition
  PageGearCheckTarget.lua # вкладка Gear check (target)
  PageHistory.lua     # вкладка History
  PageSync.lua        # вкладка Synchronization
  PageSettings.lua    # вкладка Settings
  PageInfo.lua        # вкладка Info
  ExporterWindow.lua  # оболочка главного окна (меню, заголовок, статус, вкладки)
scripts/
  generate-gear-check-bis.js # объединение BiS ID из веб-приложения → GearCheckBis.lua
docs/
  Architecture.md     # порядок TOC, слои, SavedVariables, API обновления
  UI-Views.md         # ASCII-схемы каждого вида + таблица layout-версий
  UI-Sizes.md         # размеры окна и элементов
  Gear-Check-Progress.md # фазы Gear Check (план / готово / как тестировать)
  Reputation.md       # модель личного рейтинга / событий / мемо
  Raid-Composition.md # классы и спеки, которые проверяет анализ состава
types/
  CharacterExport.ts  # TypeScript-типы JSON вкладки Export
  CooldownsExport.ts  # TypeScript-типы JSON КД аккаунта
  GearCheck.ts        # TypeScript-типы нормализованного отчёта Gear Check
```

## Заметки

- Связи хранятся в `RaidwiseDB.characterGroups`, локальный выбор мейна — отдельно в `RaidwiseDB.localCharacterMains`. Старые настройки мейна переносятся автоматически. API `GetSharedCharacterLinks` описан в [Reputation.md](docs/Reputation.md).
- Неполные и недоступные сканы явно помечены и не учитываются как готовые в рейде/мини-карте. Неподтверждённые пустые гнёзда требуют повторного ответа осмотра перед выводом об отсутствующих камнях.

- Целевая сборка: **3.3.5a** (клиенты приватных серверов используют `## Interface: 30300`).
- Сохранённые переменные лежат в `RaidwiseDB` (`WTF/Account/.../SavedVariables/`). Настройки старого `MrcExporterDB` переносятся при первой загрузке. КД и валюта персонажей для таблицы кулдаунов — в `RaidwiseDB.characters` (`.lockouts`, `.currency`). Встречи в группе и рейде — в `RaidwiseDB.history` (ключ — GUID), включая личные рейтинги (`.rating.personal` с мнением/тегами/фактами), события (`.events`), заметки (`.notes`), журнал изменений (`.changes`) и счётчик совместных групп (`.meetCount`). Язык интерфейса — `RaidwiseDB.locale` (`enUS` или `ruRU`). Стартовая вкладка меню — `RaidwiseDB.startupTab`. Флаги подсказок — `RaidwiseDB.tooltip`.
- `## X-LastUpdated` в `.toc` задаётся вручную; бейдж в README нужно держать в синхроне.
- JSON и текстовые экспорты персонажа, КД, Gear Check и синхронизации содержат версию формата. Импорт синхронизации отклоняет неизвестный `reportVersion`; личные мемо никогда не экспортируются.
- Необязательная зависимость: **GearScore** (`## OptionalDeps`) для поля `gearScore` в экспорте.
- Пересборка ID для оценки S: `node scripts/generate-gear-check-bis.js` (читает пресеты соседнего веб-репо `Raidwise`; см. `docs/Gear-Check-Surface-From-BiS.md`).
